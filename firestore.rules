rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /clients/{clientId} {
      // Any authenticated user can manage all clients (this is the admin).
      // The client can only read their own data.
      allow read, write: if request.auth != null;
    }
  }
}
